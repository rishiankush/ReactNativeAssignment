import { StyleSheet } from 'react-native'
import Color from '../../assets/colors/Color'

export default StyleSheet.create({
  SafeAreaView1: { backgroundColor: Color.BLUE, flex: 0 },
  SafeAreaView2: { flex: 1, backgroundColor: Color.BLUE },
})
